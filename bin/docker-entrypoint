#!/bin/bash -e

echo "=== ENTRYPOINT RUNNING ==="
echo "RAILS_ENV=$RAILS_ENV"
echo "DATABASE_URL=$(echo $DATABASE_URL | head -c 50)..."

# Skip db:prepare for now - will run manually
# if [ "${@: -2:1}" == "./bin/rails" ] && [ "${@: -1:1}" == "server" ]; then
#   ./bin/rails db:prepare
# fi

exec "${@}"
